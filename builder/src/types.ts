export type PageContentParams = [ string, string[], string[], string[] ];

export interface SiteConfig {
  title: string;
  url: string;
  description: string;
  author: string;
  language: string;
}
