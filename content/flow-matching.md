# Flow Matching

## Conditional probability path

Given a point P on a 2D plane we can define a conditional probability path, ie. a transformation of
an initial probability distribution (a standard Gaussian) into the Dirac delta distribution for P
(which when sampled always gives the point P):

[[ conditional-prob-path-widget ]]
